expr = atom

atom = integer / boolean

integer "integer"
  = digits:('-'? [0-9]+) { return parseFloat(digits.join(""), 10); }

  
boolean "boolean"
  = "true" { return true; }
  / "false" { return false; }

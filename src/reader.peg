expr = atom

atom = integer / boolean / string

integer "integer"
  = digits:('-'? [0-9]+) { return parseFloat(digits.join(""), 10); }

  
boolean "boolean"
  = "true" { return true; }
  / "false" { return false; }

  
string "string"
  = '"' content:[^"]* '"' { return content.join(""); }
